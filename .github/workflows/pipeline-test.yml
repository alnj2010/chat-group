name: pipeline to test pg containers
on:
  push:
    branches: [feature/github-action-containers]
  workflow_dispatch:

jobs:
  Avoid_Redundancy:
    runs-on: ubuntu-latest
    steps:
      - name: Cancel Previous Redundant Builds
        uses: styfle/cancel-workflow-action@0.9.1
        with:
          access_token: ${{ github.token }}

  Check_E2E_Test:
    runs-on: ubuntu-latest
    services:
        postgres:
          image: postgres
          env:
            POSTGRES_USER: admin
            POSTGRES_PASSWORD: mypassword
            POSTGRES_DB: chat_group_e2e_tests
          options: >-
            --health-cmd pg_isready
            --health-interval 10s
            --health-timeout 5s
            --health-retries 5
          ports:
            - 5432:5432
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "18"
      - name: Install dependencies
        run: npm ci
      - name: Connect to PostgreSQL
        run: npm run seed
        env:
            POSTGRES_PASSWORD: "mypassword"
            POSTGRES_HOST: "localhost"
            POSTGRES_PORT: 5433
            POSTGRES_DATABASE: "chat_group_e2e_tests"
            POSTGRES_USER: "admin"

